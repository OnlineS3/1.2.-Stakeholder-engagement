import React from 'react';
import { Link } from 'react-router';

var Header = props => {
    const { logged_in } = props;
    const style = {
      width: '100%',
      background: "blue",
      height: "40px"
    }
    var user_menu;
    return (
      <div style={style}>

      </div>
    )
}
export default Header;
