import React from 'react';
import { Link } from 'react-router';

var Header = props => {
    const { logged_in } = props;
    var user_menu
    return (
      <div>

      </div>
    )
}
export default Header;
